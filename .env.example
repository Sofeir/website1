# Адрес, на который форма отправляет заявку (POST, JSON: name, phone, comment).
# Пока переменная не задана, форма работает в демонстрационном режиме
# и ничего никуда не отправляет.
VITE_LEAD_ENDPOINT=
